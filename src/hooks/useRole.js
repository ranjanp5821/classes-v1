/**
 * useRole.js — Convenience re-export
 *
 * Usage:
 *   import { useRole } from "../hooks/useRole";
 *   const { activeRoleId, activeRoleConfig, selectRole, clearRole } = useRole();
 */
export { useRoleContext as useRole } from "../context/RoleContext";
